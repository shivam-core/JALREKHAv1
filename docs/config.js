// Edit only this file to change settings.
window.JALREKHA_CONFIG = {
  // Leave "" to use the pre-computed files in docs/data (works with no backend).
  // After deploying the Space, paste its URL, e.g. "https://simarsinghlamba-jalrekha-api.hf.space"
  API_URL: "/api",
  BASEMAP: "https://basemaps.cartocdn.com/gl/dark-matter-gl-style/style.json",
  TERRAIN: true,            // set false if the 3-D terrain is slow on the demo laptop
  TERRAIN_EXAGGERATION: 1.5,
  RIVER_POINT: { lat: 18.53, lon: 73.86, name: "Mula-Mutha river, Pune" },
  MAX_LEVEL: 8,
};
