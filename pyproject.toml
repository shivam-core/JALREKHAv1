[project]
name = "jalrekhav1"
version = "1.0.0"
description = "JALREKHA: When routes change, plans should too."
requires-python = ">=3.12,<3.13"
dependencies = [
    "fastapi>=0.111.0",
    "pydantic>=2.7.0",
    "networkx>=3.3",
    "uvicorn>=0.30.0",
]
